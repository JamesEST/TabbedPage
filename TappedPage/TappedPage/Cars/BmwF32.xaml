<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:TappedPage;assembly=TappedPage"
             x:Class="TappedPage.Cars.BmwF32"
             Title="BMW F32">
    
    <StackLayout>
        <Image x:Name="_ImageLogo" HeightRequest="200" Source="f32.jpg"/>
        <Button x:Name="_btn" Text="Запустить лого компании" Clicked="_btn_OnClicked"></Button>
        <ListView x:Name="phonesList"
                  HasUnevenRows="True"
                  ItemsSource="{Binding Phones}"
                  ItemTapped="OnItemTapped">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <ViewCell.View>
                            <StackLayout>
                                <Label Text="{Binding Company}" FontSize="20" />
                                <Label Text="{Binding Title}" />
                               
                            </StackLayout>
                        </ViewCell.View>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</ContentPage>










