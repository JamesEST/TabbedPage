<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TappedPage.Cars.ToyotaRav4"
             Title="Rav4">
             
    <StackLayout>
        <Image x:Name="_ImageLogo" HeightRequest="200" Source="ToyotaRav4.jpg"/>
        <Button x:Name="_btn" Text="Запустить лого компании" Clicked="_btn_OnClicked"></Button>
        <ListView x:Name="phonesList"
                  HasUnevenRows="True"
                  ItemsSource="{Binding Phones}"
                  ItemTapped="OnItemTapped">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ImageCell
                        ImageSource="{Binding ImagePath}"
                        Text="{Binding Title}"
                        Detail="{Binding CarInfo}"
                        TextColor="Black"
                        DetailColor="gray"
                    />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</ContentPage>